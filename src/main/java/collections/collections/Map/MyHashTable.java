package collections.collections.Map;

import java.util.Hashtable;

public class MyHashTable {
    // it is sync unlike HashMap
    // stores the key as an object , every object has a code, every code is stored as hashCode  (32 bit code)
   public Hashtable myHashTable = new Hashtable<>();

}
